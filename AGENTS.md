# AGENTS.md

## Agent skills

### Issue tracker

issue と spec は `.scratch/<feature>/` 配下の markdown ファイルとして管理する。詳細は `docs/agents/issue-tracker.md` を参照。

### Triage labels

triage ラベルは 5 つの標準ロール（`needs-triage` / `needs-info` / `ready-for-agent` / `ready-for-human` / `wontfix`）をそのまま使う。詳細は `docs/agents/triage-labels.md` を参照。

### Domain docs

single-context 構成。ルートの `CONTEXT.md` と `docs/adr/` を使う。詳細は `docs/agents/domain.md` を参照。
